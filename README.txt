cromlech.i18n
=============

``cromlech.i18n`` is a thin layer above zope.i18n, providing a
flexible way to register and resolve language preferences into an
application. It also proposes a default implementation that gets the
translations service up and ready out-of-the-box.
